name: imago

description: Image inspection and processing toolkit with HTTP API and CLI support.

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  path: ^1.9.0
  image: ^4.10.1
  river: ^1.1.0
  exif_reader: ^4.2.0
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6